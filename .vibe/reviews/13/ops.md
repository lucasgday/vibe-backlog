# Ops/Release Pass

- CLI surface expanded with `vibe status` (additive change).
- Existing preflight output remains, with extra sections for ongoing/hygiene visibility.
- No dependency changes; rollout risk low and scoped to output formatting.
